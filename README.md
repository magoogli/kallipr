To run the backend, open the solution found in the backend folder in visual studio.
Compile the solution and hit run.
A SqlLite database will be automatically provisioned for you and the backend should run at https://localhost:7264/

to run the front end:
run:

pnpm i
pnpm dev

in the frontend folder.
This should spin up the frontend at http://localhost:5173/
